class Solution:
    def majorityElement(self, arr):
        n = len(arr)
        freq = {}
        for num in arr:
            freq[num] = freq.get(num, 0) + 1
            if freq[num] > n // 2:
                return num
        return -1
