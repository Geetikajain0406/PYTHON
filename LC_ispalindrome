class Solution:
    def isPalindrome(self, s: str) -> bool:
        s1=[]
        for i in s:
            if i.isalnum():
                s1.append(i.lower())
        s2="".join(s1)
        s3=s2[::-1]
        if s3==s2:
            return True
        else:
            return False
        
