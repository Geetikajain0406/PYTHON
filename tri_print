def generates_tri(n):
    for i in range(1,n+1):
     for j in range (i):
       print("* ",end = "")
     
     print()
    
     
n = int(input("rows?"))
print(f"the triangle of {n}:{generates_tri(n)}"
